# Copy to .env.production and set your production API host.
# Required when the site is served over HTTPS or from a different domain than the API.
# Vue CLI bakes these into the build (npm run build).
#
# VUE_APP_API_URL=https://api.pooltool.io
# VUE_APP_WS_URL=wss://api.pooltool.io/ws
